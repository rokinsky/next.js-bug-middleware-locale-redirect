/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  i18n: {
    locales: [
      "en",
      "nl-nl",
    ],
    defaultLocale: "en",
  },
}

module.exports = nextConfig
